let browsers = ["Chrome", "edge", "brave"]; // preferred way to create an array

let score = new Array(5); // Array constructor // length

let score2 = new Array(1, 3,4,5); // elements

let numbers = new Array(1, 2, 3, 4, 5); 
console.log(numbers.length);









